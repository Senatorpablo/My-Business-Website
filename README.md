# My Business Website

Opticore SaaS landing page.

## Files

- `index.html` — main page
- `styles.css` — stylesheet

## Usage

Open `index.html` in a browser, or serve locally:

```bash
python -m http.server 8080
```
